mediana <- function(vetor){
  ordenado <- sort(vetor)##ordena o meu vetor
  if length(){
    meio <- (length(vetor)/2)+0.5 ##valor do meio do vetor, num inteiro (divisao de par)
    num <- ordenado[meio] ##vai receber o valor do meio
  }
}else{
    meio <- (length(vetor)/2)
    med <- c(ordenado[meio],ordenado[meio+1])
    num <- media(med)
} return(num)


